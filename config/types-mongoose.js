const StringBasic = { type: String }
const BooleanFalse = { type: Boolean, default: false }
const DateNow = { type: Date, default: Date.now }

module.exports = {
  StringBasic,
  BooleanFalse,
  DateNow
}